import Vue from 'vue'
import Vuex from 'vuex'
import api, {baseURL} from './api/main'


Vue.use(Vuex)

const store = new Vuex.Store({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
    },
})
export default store
